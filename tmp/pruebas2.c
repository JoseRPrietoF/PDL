// Ejemplo sencillo del uso de operadores aritmeticos.
// Debe devolver el mismo numero elevado al cuadrado

{ 
	int x; int y; bool z; 

  //z = true;
  x = 1;
  z = !(x == 1) || true;
  print(z);
  if (!(x != 1) && true){
	print(99);  
	}else{}
  while (z) {
    read(x);
    if ((x > 0) )  print(1);          
    elseif ((x < 0)) print(0);
    elseif (!(x != 0)) z = false;
    else {}
  }
}
